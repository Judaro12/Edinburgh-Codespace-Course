// ask user to enter first Number
const firstNumber = Number (window.prompt("Please type Number 6","6"));

// ask user to enter second Number 
const secondNumber = Number (window.prompt("Please type Numebr 2","2"));

// Perform the Division
const divvResult = firstNumber / secondNumber


// print result on Screen 
console.log(`The division result is: ${divvResult}`);

//show the result on a window,promp
alert(`The division of the first number and the second number is: ${divvResult}`);

